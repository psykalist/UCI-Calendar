{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const i=JSON.parse(d);const f=(i.tool_input&&(i.tool_input.file_path||i.tool_input.filePath))||'';if(!f||f.includes('.claude')||f.includes('CHANGELOG')||f.includes('changelog')||f.includes('memory')||f.endsWith('data.json')||f.endsWith('sw.js'))return;console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'REMINDER: You just edited a project file. Before committing, make sure to: (1) bump APP_VERSION in index.html to the next version number, (2) add an entry to CHANGELOG.md describing what changed, (3) update changelog.json with the new version entry. Skip this only if the edit was to data.json or sw.js cache name.'}}));}catch(e){}});\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
